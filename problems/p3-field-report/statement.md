# 田块巡检报告

巡检日志由若干行文本组成（读到 EOF 为止）。请统计并输出一行汇总。

**输入**：若干行文本，可能为空。

**输出**：一行，格式为 `lines=X words=Y chars=Z`，其中：
- $X$：行数；
- $Y$：单词总数（以空白字符分隔）；
- $Z$：字符总数（不含换行符，含单词之间的空格）。

示例：输入 `hello world
smart farm
`，共 2 行 4 个单词、21 个字符（含 1 个单词间空格…以实际统计为准），输出 `lines=2 words=4 chars=21`。
