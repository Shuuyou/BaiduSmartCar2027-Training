# 水塔水位巡检

小车依次读取 $n$ 个水塔的水位读数（0–100 的整数），按阈值分级并统计：低于 30 为 `low`，30 到 70 之间（含）为 `ok`，高于 70 为 `full`。

**输入**：一行，$n$ 个整数（$1 \le n \le 100$），空格分隔。

**输出**：前 $n$ 行，第 $i$ 行为 `tower_i: 级别`；最后一行为统计 `low=X ok=Y full=Z`。
